/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
*/
import React from 'react';

// This component is no longer in active use and is kept to avoid file deletion.
// The Virtual Ambassador tool has been repurposed into the UGC Video Generator,
// which now uses the general VideoGeneratorResultsScreen.

const VirtualAmbassadorResultsScreen: React.FC = () => {
    return (
        <div>
            {/* This screen is deprecated. */}
        </div>
    );
};

export default VirtualAmbassadorResultsScreen;